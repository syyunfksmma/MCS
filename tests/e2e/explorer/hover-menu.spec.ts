import { test } from '@playwright/test';

test.describe('Explorer Hover Quick Menu', () => {
  test.skip('hover menu interactions pending implementation', async () => {
    // Implementation will be added when ExplorerHoverMenu is wired to live data.
  });
});
