<Window x:Class="CAM_API.Feature.UI.Views.TagSelectedFacesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tag Selected Faces" Height="200" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Select Description:" Margin="0,0,0,10" />
        <ComboBox ItemsSource="{Binding DescriptionOptions}"
                  SelectedItem="{Binding SelectedDescription}"
                  Width="200" Margin="0,0,0,20" />
        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
            <Button Content="Apply" Command="{Binding ApplyCommand}" Margin="5" Width="100" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5" Width="100" />
        </StackPanel>
    </StackPanel>
</Window>
