<Window x:Class="CAM_API.Feature.UI.Views.FeatureSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:CAM_API.Common.Converters"
        Title="설비 선택" Height="250" Width="400">

    <Window.Resources>
        <conv:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="설비를 선택하세요:" FontWeight="Bold" Margin="0 0 0 10" />
        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="rbCNC" Content="CNC"
                        IsChecked="{Binding SelectedFacility, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=CNC, Mode=TwoWay}"
                        GroupName="FacilityGroup"/>
            <CheckBox Content="Option : OD 가공" IsChecked="{Binding IsOdCut, Mode=TwoWay}" Margin="15,0,0,0" Visibility="{Binding IsChecked, ElementName=rbCNC, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>
        <RadioButton Content="MCT"
                     IsChecked="{Binding SelectedFacility, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=MCT, Mode=TwoWay}"
                     GroupName="FacilityGroup"/>
        <RadioButton Content="MCT with RT"
                     IsChecked="{Binding SelectedFacility, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter= MCT_RT, Mode=TwoWay}"
                     GroupName="FacilityGroup"/>
        <RadioButton Content="MTM"
                    IsChecked="{Binding SelectedFacility, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=MTM, Mode=TwoWay}"
                    GroupName="FacilityGroup"/>
        
        <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Center">
            <Button Content="Apply" Width="100" Margin="5" Command="{Binding ApplyCommand}" />
            <Button Content="Cancel" Width="100" Margin="5" Command="{Binding CancelCommand}" />
        </StackPanel>
    </StackPanel>
</Window>
