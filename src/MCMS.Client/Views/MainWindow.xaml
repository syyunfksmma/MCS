<Window x:Class="MCMS.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MCMS" Height="600" Width="1000">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    <Grid Background="{StaticResource PrimaryBrush}">
        <Border Margin="20" Background="White" CornerRadius="12" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0 0 0 12">
                    <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" />
                    <Border Background="{StaticResource AccentBrush}" Padding="12 4" CornerRadius="8" Margin="12 0 0 0">
                        <TextBlock Text="데이터 연동" Foreground="Black" FontWeight="SemiBold" />
                    </Border>
                    <Button Content="새로고침" Command="{Binding RefreshCommand}" Margin="20 0 0 0" />
                </StackPanel>

                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="로딩 중..."
                           Foreground="#FF666666" FontStyle="Italic" Margin="0 0 0 8">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding ErrorMessage}"
                           Foreground="Red" Margin="0 0 0 12">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Static sys:String.Empty}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Grid.Row="3" Margin="0 0 20 0">
                    <TextBlock Text="품목 목록" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 8" />
                    <ListBox ItemsSource="{Binding Items}"
                             SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                             BorderBrush="#FFE0E0E0" BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="6">
                                    <TextBlock Text="{Binding ItemCode}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Name}" FontSize="12" Foreground="#FF666666" />
                                    <TextBlock Text="{Binding LatestRevision, StringFormat='Rev: {0}'}" FontSize="12" Foreground="#FF909090" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="1">
                    <TextBlock Text="선택된 Routing" FontSize="16" FontWeight="SemiBold" Margin="0 0 0 8" />
                    <DataGrid ItemsSource="{Binding SelectedItem.Routings}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              BorderBrush="#FFE0E0E0" BorderThickness="1"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Routing" Binding="{Binding RoutingCode}" Width="*" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" />
                            <DataGridTextColumn Header="CAM Rev" Binding="{Binding CamRevision}" Width="120" />
                            <DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="160" />
                            <DataGridTextColumn Header="By" Binding="{Binding UpdatedBy}" Width="120" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>